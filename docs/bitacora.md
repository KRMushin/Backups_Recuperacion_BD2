
# Bitácora de Actividades

| No. | Fecha y hora | Actividad | Comando utilizado | Resultado | Evidencia |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 15/08/2026 09:00 | Creación de la base de datos y backup base | docker exec -it hotel_mysql sh <br />mysql -u root -p <br> USE hotel_db;<br>SOURCE /scripts/00_creacion_bd.sql;<br>SHOW TABLES;<br> exit; <br> mysqldump -u root -p"$MYSQL_ROOT_PASSWORD" --no-data --routines --triggers hotel_db > /backups/incremental/backup_base.sql <br>mysql -u root -p <br>FLUSH LOGS;<br>SHOW BINARY LOGS; |  Las base de datos fue creada correctamente con las 6 tablas, backup_base.sql fue generado, tambien se aplico FLUSH LOGS y se mostraron los logs binary para mostrar el resultado obtenido | Captura No. 1 |
| 2 | 15/08/2026 10:15 | Carga del día 1, consultas y creación de backups | Cargar el archivo:<br>docker exec -it hotel_mysql sh<br>mysql -u root -p<br>USE hotel_db;<br /> SOURCE scripts/01_carga_dia1.sql;<br><br> Verificar la inserción:<br>SELECT \* FROM CLIENTE; <br />SELECT COUNT(\*) AS total_clientes FROM CLIENTE;<br>SELECT \* FROM HABITACION;<br /> SELECT COUNT(\*) AS total_habitaciones FROM HABITACION;<br>SELECT \* FROM EMPLEADO; <br />SELECT COUNT(\*) AS total_empleados FROM EMPLEADO;<br><br>backup completo: <br>docker exec -it hotel_mysql sh<br>mysqldump -u root -p"$MYSQL_ROOT_PASSWORD" --databases hotel_db --routines --triggers --single-transaction > backups/full/full_dia1.sql<br><br>backup incremental:<br>docker exec -it hotel_mysql sh<br>mysql -u root -p <br/>SHOW BINARY LOGS;<br>docker exec -it hotel_mysql sh<br>mysqlbinlog /var/lib/mysql/mysql-bin.000004 > backups/incremental/incremental_dia1.sql<br>mysql -u root -p -e "FLUSH LOGS;"<br><br>Registro tamaño fecha y hora:<br>ls -lh backups/full/full_dia1.sql<br>ls -lh backups/incremental/incremental_dia1.sql | Correcto: 150 registros cargados (50 CLIENTE, 50 HABITACION, 50 EMPLEADO); <br />backups:<br />full_dia1.sql  incremental_dia1.sql | Captura No. 2 |
| 3 | 15/08/2026 11:30 | Carga del día 2, consultas y creación de backups | Cargar el archivo:<br/>docker exec -it hotel_mysql sh<br/>mysql -u root -p<br/>USE hotel_db;<br /> SOURCE scripts/02_carga_dia2.sql;<br/><br/> Verificar la inserción:<br/>SELECT \* FROM CLIENTE; <br />SELECT COUNT(\*) AS total_clientes FROM CLIENTE;<br/>SELECT \* FROM HABITACION;<br /> SELECT COUNT(\*) AS total_habitaciones FROM HABITACION;<br/>SELECT \* FROM EMPLEADO; <br />SELECT COUNT(\*) AS total_empleados FROM EMPLEADO;<br/>SELECT * FROM RESERVA;<br/>SELECT COUNT(*) AS total_reservas FROM RESERVA;<br/>backup completo: <br/>docker exec -it hotel_mysql sh<br/>mysqldump -u root -p"$MYSQL_ROOT_PASSWORD" --databases hotel_db --routines --triggers --single-transaction > backups/full/full_dia2.sql<br/><br/>backup incremental:<br/>docker exec -it hotel_mysql sh<br/>mysql -u root -p -e "SHOW BINARY LOGS;"<br/>docker exec -it hotel_mysql sh<br/>mysqlbinlog /var/lib/mysql/mysql-bin.000005 > backups/incremental/incremental_dia2.sql<br/>mysql -u root -p -e "FLUSH LOGS;"<br/><br/>Registro tamaño fecha y hora:<br/>ls -lh backups/full/full_dia2.sql<br/>ls -lh backups/incremental/incremental_dia2.sql | Correcto: +50 registros en RESERVA con FKs válidas; full_dia2.sql e incremental_dia2.sql creados | Captura No. 3 |
| 4 | 15/08/2026 12:45 | Carga del día 3, consultas y creación de backups | Cargar el archivo:<br/>docker exec -it hotel_mysql sh<br/>mysql -u root -p<br/>USE hotel_db;<br /> SOURCE scripts/03_carga_dia3.sql;<br/><br/> Verificar la inserción:<br/>SELECT \* FROM CLIENTE; <br />SELECT COUNT(\*) AS total_clientes FROM CLIENTE;<br/>SELECT \* FROM HABITACION;<br /> SELECT COUNT(\*) AS total_habitaciones FROM HABITACION;<br/>SELECT \* FROM EMPLEADO; <br />SELECT COUNT(\*) AS total_empleados FROM EMPLEADO;<br/>SELECT * FROM RESERVA;<br/>SELECT COUNT(*) AS total_reservas FROM RESERVA;<br/>SELECT * FROM LOG_HABITACION;<br/>SELECT COUNT(*) AS total_logs FROM LOG_HABITACION;<br/>backup completo: <br/>docker exec -it hotel_mysql sh<br/>mysqldump -u root -p"$MYSQL_ROOT_PASSWORD" --databases hotel_db --routines --triggers --single-transaction > backups/full/full_dia3.sql<br/><br/>backup incremental:<br/>docker exec -it hotel_mysql sh<br/>mysql -u root -p -e "SHOW BINARY LOGS;"<br/>docker exec -it hotel_mysql sh<br/>mysqlbinlog /var/lib/mysql/mysql-bin.000006> backups/incremental/incremental_dia3.sql<br/>mysql -u root -p -e "FLUSH LOGS;"<br/><br/>Registro tamaño fecha y hora:<br/>ls -lh backups/full/full_dia3.sql<br/>ls -lh backups/incremental/incremental_dia3.sql | Correcto: +50 registros en LOG_HABITACION ( primera carga ); full_dia3.sql e incremental_dia3.sql creados | Captura No. 4 |
| 5 | 15/08/2026 4:00 | Carga del día 4, consultas y creación de backups | Cargar el archivo:<br/>docker exec -it hotel_mysql sh<br/>mysql -u root -p<br/>USE hotel_db;<br /> SOURCE scripts/04_carga_dia4.sql;<br/><br/> Verificar la inserción:<br/>SELECT \* FROM CLIENTE; <br />SELECT COUNT(\*) AS total_clientes FROM CLIENTE;<br/>SELECT \* FROM HABITACION;<br /> SELECT COUNT(\*) AS total_habitaciones FROM HABITACION;<br/>SELECT \* FROM EMPLEADO; <br />SELECT COUNT(\*) AS total_empleados FROM EMPLEADO;<br/>SELECT * FROM RESERVA;<br />SELECT COUNT(*) AS total_reservas FROM RESERVA;<br />SELECT * FROM LOG_HABITACION;<br />SELECT COUNT(*) AS total_logs FROM LOG_HABITACION;<br />SELECT * FROM PAGO;<br />SELECT COUNT(*) AS total_pagos FROM PAGO;<br/>backup completo: <br/>docker exec -it hotel_mysql sh<br/>mysqldump -u root -p"$MYSQL_ROOT_PASSWORD" --databases hotel_db --routines --triggers --single-transaction > backups/full/full_dia4.sql<br/><br/>backup incremental:<br/>docker exec -it hotel_mysql sh<br/>mysql -u root -p -e "SHOW BINARY LOGS;"<br/>docker exec -it hotel_mysql sh<br/>mysqlbinlog /var/lib/mysql/mysql-bin.000007 > backups/incremental/incremental_dia4.sql<br/>mysql -u root -p -e "FLUSH LOGS;"<br/><br/>Registro tamaño fecha y hora:<br/>ls -lh backups/full/full_dia4.sql<br/>ls -lh backups/incremental/incremental_dia4.sql | Correcto: +50 registros en PAGO; full_dia4.sql e incremental_dia4.sql creados |                |
|      |                  |                                                  | Cargar el archivo:<br/>docker exec -it hotel_mysql sh<br/>mysql -u root -p<br/>USE hotel_db;<br /> SOURCE scripts/05_carga_dia5.sql;<br/><br/> Verificar la inserción:<br/>SELECT \* FROM CLIENTE; <br />SELECT COUNT(\*) AS total_clientes FROM CLIENTE;<br/>SELECT \* FROM HABITACION;<br /> SELECT COUNT(\*) AS total_habitaciones FROM HABITACION;<br/>SELECT \* FROM EMPLEADO; <br />SELECT COUNT(\*) AS total_empleados FROM EMPLEADO;<br/>SELECT * FROM RESERVA;<br/>SELECT COUNT(*) AS total_reservas FROM RESERVA;<br/><br/>SELECT * FROM PAGO;<br/>SELECT COUNT(*) AS total_pagos FROM PAGO;<br/><br/>SELECT * FROM LOG_HABITACION;<br/>SELECT COUNT(*) AS total_logs FROM LOG_HABITACION;<br/>backup completo: <br/>docker exec -it hotel_mysql sh<br/>mysqldump -u root -p"$MYSQL_ROOT_PASSWORD" --databases hotel_db --routines --triggers --single-transaction > backups/full/full_dia5.sql<br/><br/>backup incremental:<br/>docker exec -it hotel_mysql sh<br/>mysql -u root -p -e "SHOW BINARY LOGS;"<br/>docker exec -it hotel_mysql sh<br/>mysqlbinlog /var/lib/mysql/mysql-bin.000008> backups/incremental/incremental_dia5.sql<br/>mysql -u root -p -e "FLUSH LOGS;"<br/><br/>Registro tamaño fecha y hora:<br/>ls -lh backups/full/full_dia5.sql<br/>ls -lh backups/incremental/incremental_dia5.sql | Correcto: +50 registros en LOG_HABITACION ( segunda carga ); full_dia5.sql e incremental_dia5.sql creados | Captura No. 6 |
| 7 | 16/08/2026 09:00 | Restauración del backup completo del día 1 | docker exec -it hotel_mysql mysql -u root -p -e "DROP DATABASE hotel_db;"<br>docker exec -it hotel_mysql mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS hotel_db;"<br>time docker exec -i hotel_mysql mysql -u root -p < backups/full/full_dia1.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: datos del Día 1 restaurados y verificados (HABITACION/EMPLEADO completos; RESERVA/PAGO/LOG en 0, esperado); tiempo anotado en tabla_tiempos.md | Captura No. 7 |
| 8 | 16/08/2026 09:40 | Restauración del backup completo del día 2 | DROP DATABASE hotel_db; / CREATE DATABASE IF NOT EXISTS hotel_db;<br>time docker exec -i hotel_mysql mysql -u root -p < backups/full/full_dia2.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: datos del Día 2 restaurados y verificados (incluye RESERVA); tiempo anotado en tabla_tiempos.md | Captura No. 8 |
| 9 | 16/08/2026 10:20 | Restauración del backup completo del día 3 | DROP DATABASE hotel_db; / CREATE DATABASE IF NOT EXISTS hotel_db;<br>time docker exec -i hotel_mysql mysql -u root -p < backups/full/full_dia3.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: datos del Día 3 restaurados y verificados (incluye LOG_HABITACION 1.ª carga); tiempo anotado en tabla_tiempos.md | Captura No. 9 |
| 10 | 16/08/2026 11:00 | Restauración del backup completo del día 4 | DROP DATABASE hotel_db; / CREATE DATABASE IF NOT EXISTS hotel_db;<br>time docker exec -i hotel_mysql mysql -u root -p < backups/full/full_dia4.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: datos del Día 4 restaurados y verificados (incluye PAGO); tiempo anotado en tabla_tiempos.md | Captura No. 10 |
| 11 | 16/08/2026 11:40 | Restauración del backup completo del día 5 | DROP DATABASE hotel_mysql mysql -u root -p -e "DROP DATABASE hotel_db;"<br>docker exec -it hotel_mysql mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS hotel_db;"<br>time docker exec -i hotel_mysql mysql -u root -p < backups/full/full_dia5.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: estado final del Día 5 restaurado y verificado (6 tablas completas); tiempo anotado en tabla_tiempos.md | Captura No. 11 |
| 12 | 16/08/2026 12:30 | Restauración del incremental del día 1 | docker exec -it hotel_mysql mysql -u root -p -e "DROP DATABASE hotel_db;"<br>docker exec -it hotel_mysql mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS hotel_db;"<br>time docker exec -i hotel_mysql mysql -u root -p < backups/incremental/backup_base.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia1.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: cadena backup_base → incremental_dia1 aplicada en orden; datos del Día 1 verificados; tiempo anotado en tabla_tiempos.md | Captura No. 12 |
| 13 | 16/08/2026 13:10 | Restauración del incremental del día 2 | DROP DATABASE hotel_db; / CREATE DATABASE IF NOT EXISTS hotel_db;<br>time docker exec -i hotel_mysql mysql -u root -p < backups/incremental/backup_base.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia1.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia2.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: cadena backup_base → incremental_dia1 → incremental_dia2 aplicada; datos del Día 2 verificados; tiempo anotado en tabla_tiempos.md | Captura No. 13 |
| 14 | 16/08/2026 13:50 | Restauración del incremental del día 3 | DROP DATABASE hotel_db; / CREATE DATABASE IF NOT EXISTS hotel_db;<br>time docker exec -i hotel_mysql mysql -u root -p < backups/incremental/backup_base.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia1.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia2.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia3.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: cadena incremental hasta incremental_dia3 aplicada; datos del Día 3 verificados; tiempo anotado en tabla_tiempos.md | Captura No. 14 |
| 15 | 16/08/2026 14:30 | Restauración del incremental del día 4 | DROP DATABASE hotel_db; / CREATE DATABASE IF NOT EXISTS hotel_db;<br>time docker exec -i hotel_mysql mysql -u root -p < backups/incremental/backup_base.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia1.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia2.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia3.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia4.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: cadena incremental hasta incremental_dia4 aplicada; datos del Día 4 verificados; tiempo anotado en tabla_tiempos.md | Captura No. 15 |
| 16 | 16/08/2026 15:10 | Restauración del incremental del día 5 | DROP DATABASE hotel_db; / CREATE DATABASE IF NOT EXISTS hotel_db;<br>time docker exec -i hotel_mysql mysql -u root -p < backups/incremental/backup_base.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia1.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia2.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia3.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia4.sql<br>time docker exec -i hotel_mysql mysql -u root -p hotel_db < backups/incremental/incremental_dia5.sql<br>SELECT \* FROM ...; + SELECT COUNT(\*) ...; | Correcto: cadena incremental completa backup_base → incremental_dia1 … incremental_dia5 aplicada; estado final del Día 5 verificado (6 tablas completas); tiempo anotado en tabla_tiempos.md | Captura No. 16 |
| 17 | 17/08/2026 10:00 | Comparación y análisis de resultados | ls -lh backups/full/*.sql backups/incremental/*.sql<br>Redacción de tabla_tiempos.md y analisis_resultados.md | Correcto: tamaños y tiempos de restauración comparados; ventajas/desventajas de cada método y recomendación documentadas; conclusiones redactadas | Captura No. 17 |

---

## ✅ Checklist de actividades completadas

- [x] 1. Creación de la base de datos y backup base.
- [x] 2. Carga del día 1, consultas y creación de backups.
- [x] 3. Carga del día 2, consultas y creación de backups.
- [x] 4. Carga del día 3, consultas y creación de backups.
- [x] 5. Carga del día 4, consultas y creación de backups.
- [x] 6. Carga del día 5, consultas y creación de backups.
- [x] 7. Restauración del backup completo del día 1.
- [x] 8. Restauración del backup completo del día 2.
- [x] 9. Restauración del backup completo del día 3.
- [x] 10. Restauración del backup completo del día 4.
- [x] 11. Restauración del backup completo del día 5.
- [x] 12. Restauración del incremental del día 1.
- [x] 13. Restauración del incremental del día 2.
- [x] 14. Restauración del incremental del día 3.
- [x] 15. Restauración del incremental del día 4.
- [x] 16. Restauración del incremental del día 5.
- [x] 17. Comparación y análisis de resultados.
